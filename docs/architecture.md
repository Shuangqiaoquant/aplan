# APlan 项目架构简介

APlan 是一个面向沪深 A 股及创业板的可审计投资研究系统。它不让 Agent 直接“猜股票”，而是把数据、资讯、策略、风控、模拟成交和审计拆成独立模块。

当前系统处于 **研究模式**：基础框架已经建立，但尚无通过隔离验证的策略，因此不会生成可执行交易。

```mermaid
flowchart TD
    A["Tushare 行情与主数据"] --> C["原始数据快照"]
    B["巨潮法定公告与 PDF"] --> C

    C --> D{"数据质量闸门"}
    D -->|"失败"| X["停止流程并记录错误"]
    D -->|"通过"| E["标准化研究数据"]

    E --> F["量价 / 基本面策略插件"]
    E --> G["公告全文与资讯 Agent"]

    G --> H["事实、正反证据、不确定性"]
    F --> I["统一信号格式"]
    H --> I

    I --> J{"策略验证与审批"}
    J -->|"研究中或验证失败"| K["仅记录，不可执行"]
    J -->|"通过隔离验证"| L["候选目标仓位"]

    L --> M["组合与风险引擎"]
    M --> N{"风险检查"}
    N -->|"超限"| O["拒绝或缩减订单"]
    N -->|"通过"| P["纸面订单计划"]

    P --> Q{"模拟盘审批"}
    Q -->|"未批准"| K
    Q -->|"已批准"| R["下一交易日模拟成交"]

    R --> S["持仓、现金、盈亏与 T+1"]
    S --> T["每日研究报告"]
    K --> T
    O --> T
    X --> T

    T --> U["SHA-256 审计哈希链"]
```

## 六个核心层

| 层级 | 作用 | 当前状态 |
|---|---|---|
| 数据层 | 行情、主数据、公告、PDF及历史快照 | 已完成基础接入 |
| 质量层 | 重复、日期、OHLC、覆盖率及文件哈希检查 | 已完成 |
| 研究层 | 可插拔策略、公告事件及资讯 Agent | 框架完成，策略研究中 |
| 决策层 | 统一信号、证据、风险和失效条件 | 已完成 |
| 风控层 | 仓位、现金、行业、换手及回撤熔断 | 已完成 |
| 执行与审计层 | 纸面成交、T+1、报告和哈希链 | 已完成，尚未启用模拟盘 |

## 每日运行流程

```mermaid
sequenceDiagram
    participant Scheduler as 每日任务
    participant Data as 数据源
    participant Quality as 质量检查
    participant Agent as 策略与资讯 Agent
    participant Risk as 风控引擎
    participant Paper as 纸面模拟
    participant Audit as 报告与审计

    Scheduler->>Data: 下载收盘行情与公告
    Data->>Quality: 提交带时间戳的原始快照
    Quality->>Quality: 检查完整性并计算 SHA-256

    alt 数据异常
        Quality->>Audit: 记录失败并停止
    else 数据通过
        Quality->>Agent: 提供截止当日可见数据
        Agent->>Agent: 生成候选、证据和风险
        Agent->>Risk: 提交统一信号

        alt 策略未验证或未审批
            Risk->>Audit: 阻止执行，仅保留研究记录
        else 策略已验证且审批
            Risk->>Paper: 提交合规纸面订单
            Paper->>Paper: 次日开盘、成本、涨跌停、T+1
            Paper->>Audit: 更新虚拟持仓和盈亏
        end
    end

    Audit->>Audit: 生成 Markdown 报告并追加审计哈希链
```

## 三重执行闸门

任何信号要进入纸面模拟，必须同时满足：

1. **策略闸门**：通过训练集、隔离验证集和多调仓起点检查。
2. **风险闸门**：满足仓位、现金、换手、集中度和回撤规则。
3. **审批闸门**：明确获得模拟盘审批，并将运行模式切换出 `research_only`。

实盘还需要独立审批；当前系统没有连接真实券商。

## 资讯 Agent 的定位

资讯 Agent 不是“看新闻荐股”，而是把公告转换成可验证的数据：

```text
巨潮公告
→ 保存发布时间和 PDF 原文
→ 计算文件哈希
→ 提取全文
→ 识别事实、金额、日期与风险
→ 输出正反证据和不确定性
→ 进行同类事件历史回测
→ 通过验证后才允许进入策略评分
```

目前公告分析仍不会创建可执行信号。

## 当前项目位置

```mermaid
flowchart LR
    A["通用框架"] --> B["策略研究"]
    B --> C["隔离验证"]
    C --> D["纸面模拟 2-3个月"]
    D --> E["小资金实盘评估"]

    style A fill:#b7e4c7,stroke:#2d6a4f
    style B fill:#ffe8a1,stroke:#a66a00
    style C fill:#ffe8a1,stroke:#a66a00
    style D fill:#eeeeee,stroke:#777777
    style E fill:#eeeeee,stroke:#777777
```

- 通用框架：主体已完成。
- 策略研究：进行中，当前量价策略未通过验证。
- 纸面模拟：引擎已完成，但尚未初始化或启用。
- 实盘：未开始，也未连接券商。

